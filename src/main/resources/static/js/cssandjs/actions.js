/**
 * Just for testing out adding javascript to the playerView template
 */

function showAlert() {
    alert("The button was clicked!");
}

function showName(name) {
    alert("Here's the name: " + name);
}